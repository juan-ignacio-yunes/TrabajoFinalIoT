import { EstadoSueloPipe } from './estado-suelo.pipe';

describe('EstadoSueloPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoSueloPipe();
    expect(pipe).toBeTruthy();
  });
});
